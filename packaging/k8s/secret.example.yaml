# EXAMPLE ONLY. Edit with your real credentials, or better, use a secret manager
# (External Secrets Operator, sealed-secrets). Never commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: thingctx-credentials
type: Opaque
stringData:
  # e.g. an API key the gateway resolves for an HTTP Thing. Keyed per your TDs.
  EXAMPLE_SERVICE_TOKEN: "replace-me"
