{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-09-29/server.schema.json",
  "name": "com.thingctx/thingctx",
  "description": "Drive any agent against any W3C WoT Thing from its description. The agent never holds your keys; you gate what it is allowed to do.",
  "repository": {
    "url": "https://github.com/thingctx/thingctx",
    "source": "github"
  },
  "version": "0.2.0",
  "websiteUrl": "https://thingctx.com",
  "packages": [
    {
      "registryType": "pypi",
      "registryBaseUrl": "https://pypi.org",
      "identifier": "thingctx",
      "version": "0.2.0",
      "transport": { "type": "stdio" },
      "runtimeHint": "uvx",
      "packageArguments": [
        { "type": "named", "name": "--from", "value": "thingctx[mcp,http,mqtt,media,filesystem,authz]" },
        { "type": "positional", "value": "thingctx-mcp" },
        {
          "type": "positional",
          "valueHint": "tds",
          "description": "A TD Directory URL, a folder of Thing Descriptions, or a TD file. Defaults to the curated thingctx registry.",
          "isRequired": true,
          "default": "https://td.thingctx.com/index.json"
        }
      ],
      "environmentVariables": [
        {
          "name": "THINGCTX_POLICY",
          "description": "What the agent may do: read-only | full.",
          "default": "read-only",
          "isRequired": false
        },
        {
          "name": "THINGCTX_APPROVE_WHEN",
          "description": "When the agent asks before acting: declared | destructive | all | never.",
          "default": "destructive",
          "isRequired": false
        },
        {
          "name": "THINGCTX_FS_ROOT",
          "description": "Sandbox directory for the filesystem Thing. Unset = refuse every filesystem call. Google OAuth: drop Desktop client JSON at ~/.config/thingctx/oauth-clients/oauth2.googleapis.com.json.",
          "isRequired": false
        }
      ]
    }
  ]
}
